[project]
name = "lfs_help"
version = "0.0.1"
description = "Keyboard shortcuts and reference panel for LichtFeld Studio"
requires-python = ">=3.10"
dependencies = []

[tool.lichtfeld]
hot_reload = true
plugin_api = ">=1,<2"
lichtfeld_version = ">=0.4.2"
required_features = []
